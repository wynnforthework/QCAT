# 策略自动启动功能实现文档

## 概述

我们已经成功实现了完整的策略自动启动功能，现在系统可以在启动时自动启动配置为自动启动的策略。

## 实现的功能

### 1. 数据库支持
- ✅ 添加了 `auto_start` 字段到策略表
- ✅ 添加了 `startup_priority` 字段控制启动优先级
- ✅ 添加了 `last_auto_start` 字段记录最后自动启动时间
- ✅ 创建了相应的数据库索引优化查询性能

### 2. 自动启动服务 (`internal/strategy/autostart/service.go`)
- ✅ 完整的自动启动服务实现
- ✅ 支持配置化的启动参数
- ✅ 支持分批启动策略
- ✅ 支持重试机制和错误处理
- ✅ 支持统计信息收集

### 3. 配置管理 (`configs/strategy_autostart.yaml`)
- ✅ 详细的自动启动配置文件
- ✅ 支持启动延迟、并发控制、重试配置
- ✅ 支持风险控制和监控配置
- ✅ 支持故障恢复配置

### 4. API 接口
- ✅ 策略自动启动设置管理接口
- ✅ 自动启动策略列表查询接口
- ✅ 自动启动统计信息接口
- ✅ 手动触发自动启动接口

### 5. 系统集成
- ✅ 集成到多策略工作流系统
- ✅ 集成到主程序启动流程
- ✅ 集成到API服务器

## 核心特性

### 启动模式
- **自动启动**: 系统启动时自动启动标记为自动启动的策略
- **手动启动**: 通过API接口手动启动策略
- **混合模式**: 支持同时使用自动和手动启动

### 优先级控制
- **高优先级** (1-20): 核心策略，优先启动
- **中优先级** (21-80): 辅助策略，次要启动
- **低优先级** (81-100): 实验策略，最后启动

### 分批启动
- 支持分批启动策略，避免系统过载
- 可配置每批最大启动数量
- 可配置批次间隔时间

### 重试机制
- 支持启动失败时的自动重试
- 可配置重试次数和重试延迟
- 支持指数退避重试策略

### 安全控制
- 支持策略验证，只启动通过验证的策略
- 支持跳过验证失败的策略
- 支持启动超时控制

## 配置示例

```yaml
strategy_autostart:
  enabled: true                    # 启用自动启动
  startup_delay: "30s"            # 系统启动后延迟30秒
  max_concurrent_startups: 3       # 最多同时启动3个策略
  startup_timeout: "2m"           # 单个策略启动超时2分钟
  max_retries: 2                  # 最多重试2次
  retry_delay: "10s"              # 重试延迟10秒
  max_auto_start_per_batch: 3     # 每批最多启动3个策略
  batch_interval: "5s"            # 批次间隔5秒
```

## API 接口

### 更新策略自动启动设置
```http
PUT /api/v1/strategy/{id}/auto-start
Content-Type: application/json

{
  "auto_start": true,
  "startup_priority": 10
}
```

### 获取自动启动策略列表
```http
GET /api/v1/auto-start/strategies
```

### 获取自动启动统计信息
```http
GET /api/v1/auto-start/stats
```

### 手动触发自动启动
```http
POST /api/v1/auto-start/trigger
```

## 使用方法

### 1. 设置策略自动启动
通过API接口或直接修改数据库，将策略的 `auto_start` 字段设置为 `true`，并设置合适的 `startup_priority`。

### 2. 启动系统
当QCAT系统启动时，自动启动服务会：
1. 等待配置的启动延迟时间
2. 查询所有标记为自动启动的策略
3. 按优先级排序策略
4. 分批启动策略
5. 记录启动统计信息

### 3. 监控启动状态
通过API接口查询自动启动统计信息，监控启动成功率和失败情况。

## 启动流程

```
系统启动
    ↓
等待启动延迟 (30s)
    ↓
查询自动启动策略
    ↓
按优先级排序
    ↓
分批启动策略
    ↓
记录统计信息
    ↓
完成
```

## 测试验证

我们创建了测试程序验证功能：
- ✅ 配置加载和解析测试
- ✅ 策略排序和分批逻辑测试
- ✅ 数据库迁移成功执行

## 下一步

1. **前端界面**: 在前端添加策略自动启动设置界面
2. **监控面板**: 添加自动启动状态监控面板
3. **告警机制**: 添加自动启动失败告警
4. **性能优化**: 根据实际使用情况优化启动性能

## 总结

现在QCAT系统已经具备完整的策略自动启动功能：
- 🔄 **自动启动**: 系统启动时自动启动配置的策略
- 🎯 **优先级控制**: 按优先级顺序启动策略
- 📊 **统计监控**: 完整的启动统计和监控
- 🛡️ **安全控制**: 验证和重试机制保证启动安全
- 🔧 **灵活配置**: 丰富的配置选项满足不同需求

策略自动启动功能已经完全集成到系统中，可以同时执行多个策略，并且支持自动启动和手动启动两种模式。
